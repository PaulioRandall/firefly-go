package rinser

/*
import (
	"testing"

	"github.com/stretchr/testify/require"
	//"github.com/PaulioRandall/firefly-go/workflow/err"
	"github.com/PaulioRandall/firefly-go/workflow/token"
)

func tok(tt token.TokenType, v string) token.Token {
	return token.MakeToken(tt, v, token.MakeInlineRange(0, 0, 0, len(v)))
}

func Test_1_tokenStream(t *testing.T) {
	gen := token.NewTokenGenerator()
	stream := newTkReader([]token.Token{
		gen()
	})

	act, e := RinseAll(stream)

	require.Nil(t, e)
	require.Empty(t, act)
}
*/
