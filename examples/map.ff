
// Creates a map
data = map{ a: 1, b: 2 }

// Identical as above but spread over several lines
data = map{
	"a": 1,
	"b": 2,
}

// Add a new entry
data["c"] = 3

// Iterate a map
// On each loop assign k the entry key and v its value
for k, v in data {

	// Get an entry's value
	val = data[k] // val == v
	
	// Update a mapping
	data[k] = val

	// Remove a mapping
	data[k] = nothing
}